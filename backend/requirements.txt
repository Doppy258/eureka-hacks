fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
numpy>=1.26.0,<3
httpx>=0.27.0

# TRIBE v2 (optional for full inference). Requires Python 3.11+, HF token for gated Llama, GPU strongly recommended.
# Install separately when ready:
#   pip install "git+https://github.com/facebookresearch/tribev2.git"
